Algoritmo EcuacionSegundoGrado
	Definir a, b, c, discriminante, realParte, imaginariaParte, x1, x2 Como Real
	Escribir "Resolver la ecuación de segundo grado: ax^2 + bx + c = 0"
	Escribir "Ingresa el valor de a:"
	Leer a
	Escribir "Ingresa el valor de b:"
	Leer b
	Escribir "Ingresa el valor de c:"
	Leer c
	
	// Calcular el discriminante
	discriminante = b^2 - 4*a*c
	
	// Verificar si el discriminante es mayor, igual o menor que cero
	Si discriminante > 0 Entonces
		// Soluciones reales diferentes
		x1 = (-b + raiz(discriminante)) / (2 * a)
		x2 = (-b - raiz(discriminante)) / (2 * a)
		Escribir "Las soluciones reales son: "
		Escribir "x1 = ", x1
		Escribir "x2 = ", x2
	SiNo
		Si discriminante = 0 Entonces
			// Solución única real
			x1 = -b / (2 * a)
			Escribir "La solución única es: "
			Escribir "x1 = ", x1
		SiNo
			// Soluciones complejas
			realParte = -b / (2 * a)
			imaginariaParte = raiz(Abs(discriminante)) / (2 * a)
			Escribir "Las soluciones complejas son: "
			Escribir "x1 = ", realParte, " + ", imaginariaParte, "i"
			Escribir "x2 = ", realParte, " - ", imaginariaParte, "i"
		FinSi
	FinSi
FinAlgoritmo
